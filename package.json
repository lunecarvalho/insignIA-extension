{
  "name": "insignia-extension",
  "version": "1.0.0",
  "private": true,
  "description": "InsignIA Chrome Extension (TypeScript, Manifest V3)",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "watch": "node ./scripts/dev.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "esbuild": "^0.18.0",
    "typescript": "^7.0.2"
  }
}
